# Server 环境变量配置（复制为 .env 使用）

# JWT 密钥 — 必须修改为高强度随机字符串
JWT_SECRET=your-random-secret-at-least-32-chars

# MongoDB 连接
MONGO_URI=mongodb://localhost:27017/campus-maze

# CORS 允许的域名（逗号分隔）
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# 端口
PORT=3001

# Token 过期时间（可选）
JWT_EXPIRES_IN=7d
